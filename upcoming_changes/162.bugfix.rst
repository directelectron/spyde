Direct Electron ``.de5`` files open again as 4D-STEM scans, and open lazily: the EMD reader rejected the camera's column-shaped axis arrays with a ``TypeError``, read the whole datacube into memory even for a lazy load (so an acquisition stopped early tried to allocate its full planned scan), and handed the datacube back transposed with no navigation axes.
