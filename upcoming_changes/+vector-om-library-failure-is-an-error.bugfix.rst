Vector Orientation Mapping's Generate Library reports a failure as a
failure. A .cif the matcher could not build a plan from used to leave the
status at "ready (0 orientations)" with an empty IPF triangle and the
reason in the debug log only; it now raises an error naming the cause and
the caret stays on the Library tab.
